const DEFAULT_TICK_COUNT = 7;

const DURATION = {
  DAY: 'day',
  HOUR: 'hour',
  WEEK: 'week',
  MONTH: 'month',
  YEAR: 'year',
  ALL: 'all',
};

const DURATION_LIST = Object.values(DURATION);

export {
  DEFAULT_TICK_COUNT,
  DURATION,
  DURATION_LIST,
};
